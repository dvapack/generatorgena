**Необходимо полностью переделать бекенд:**
1. Изменить API
2. Добавить брокер сообщений - RabbitMQ
3. Перенести хранение файлов генерации в S3/MinIO
4. Добавить observability

**Что вообще должен делать сайтик?**
1. Позволять регистрироваться - email + password
2. Позволять входить в аккаунт
3. Позволять менять пароль
4. Позволять удалять аккаунт
5. Позволять генерировать картинку, аудио или видео по текстовому описанию
6. Позволять ставить оценку генерации
7. Позволять просматривать историю генераций
8. Позволять переходить из истории генераций к конкретной генерации
9. Позволять скачивать/отображать файл результата генерации

**Сущности в базе данных:**

| User         | GenerationRequest | GeneratedAsset |
|--------------|-------------------|----------------|
| id           | id                | id             |
| email        | userId            | requestId      |
| passwordHash | prompt            | objectKey      |
|              | type              | assetType      |
|              | status            | contentType    |
|              | rating            | sizeBytes      |
|              | createdAt         | duration       |
|              | completedAt       | width          |
|              |                   | height         |
|              |                   | createdAt      |

**Связи:**

```text
User 1 -> N GenerationRequest
GenerationRequest 1 -> 0..1 GeneratedAsset
```

`GeneratedAsset` отсутствует, пока генерация ещё не завершена.

**Основные enum:**

```text
GenerationRequest.type:
- IMAGE
- VIDEO
- AUDIO

GenerationRequest.status:
- QUEUED
- PROCESSING
- COMPLETED
- FAILED

GeneratedAsset.assetType:
- IMAGE
- VIDEO
- AUDIO
```

`GeneratedAsset.contentType` - это MIME-тип файла, например:

```text
image/png
video/mp4
audio/mpeg
```

**Хранение файлов:**

Файлы генерации хранятся в S3/MinIO.

В базе хранится только `GeneratedAsset.objectKey`, например:

```text
images/requests/111e8400-e29b-41d4-a716/result.png
```

Bucket хранится в конфигурации backend, а не в таблице.

**Методы API:**

1. `/users`
   1. `POST /login` - вход
   2. `POST /refresh` - обновление токена
   3. `POST /register` - регистрация
   4. `POST /logout` - выход
   5. `PUT /change-password` - смена пароля

2. `/generations`
   1. `POST /` - создать запрос генерации
   2. `GET /` - получить историю генераций текущего пользователя
   3. `GET /{id}` - получить конкретную генерацию и metadata результата
   4. `PUT /{id}/rating` - оценить генерацию
   5. `GET /{id}/asset` - скачать/отобразить файл результата генерации

# План реализации

## 1. Завершить слой PostgreSQL

- [x] Окончательно проверить названия и порядок аргументов репозиториев.
- [ ] Определиться с типом `sizeBytes`: оставить `Integer/int` или перейти на `Long/bigint`.
- [ ] Добавить интеграционные тесты репозиториев с PostgreSQL через Testcontainers.
- [ ] Проверить сохранение и получение пользователя.
- [ ] Проверить сохранение и получение запроса генерации.
- [ ] Проверить получение страницы запросов пользователя.
- [ ] Проверить подсчёт общего количества запросов пользователя.
- [ ] Проверить сохранение и пакетное получение ассетов.
- [ ] Проверить обновление статуса и рейтинга.
- [ ] Проверить каскадное удаление запросов и записей ассетов.

## 2. Реализовать обработку ошибок

- [x] Определить набор прикладных исключений.
- [x] Добавить исключение для отсутствующей генерации.
- [x] Добавить исключение для операции, недопустимой в текущем статусе генерации.
- [ ] Добавить исключения для недоступности RabbitMQ и S3/MinIO.
- [x] Реализовать глобальный обработчик исключений.
- [x] Обработать ошибки валидации DTO.
- [x] Обработать некорректный UUID в path-параметре.
- [x] Привести `ErrorResponse` и `docs/api.md` к одному формату.

## 3. Реализовать сервис генераций

- [x] Создать `GenerationService`.
- [x] Реализовать создание запроса генерации со статусом `QUEUED`.
- [x] Реализовать получение одной генерации с проверкой владельца.
- [x] Реализовать получение страницы генераций пользователя.
- [x] Для страницы получать ассеты одним пакетным запросом.
- [x] Для страницы получать общее количество генераций отдельным запросом.
- [x] Реализовать обновление рейтинга только для генерации в статусе `COMPLETED`.
- [x] Реализовать удаление генерации.
- [x] Расставить транзакции на изменяющих операциях.
- [ ] Добавить unit-тесты сервиса.

## 4. Реализовать REST API

- [x] Создать `GenerationController`.
- [x] Реализовать создание генерации.
- [x] Реализовать получение истории с пагинацией.
- [x] Реализовать получение одной генерации.
- [x] Реализовать изменение рейтинга.
- [x] Реализовать удаление генерации.
- [ ] Реализовать получение файла результата.
- [x] Настроить валидацию `page`, `size`, `prompt`, `type` и `rating`.
- [ ] Определить окончательные HTTP-статусы для всех операций.
- [ ] Привести примеры UUID и DTO в `docs/api.md` к фактическому API.
- [ ] Добавить тесты контроллера.

## 5. Реализовать авторизацию

Авторизацию реализовывать поэтапно. После каждого этапа приложение должно
запускаться, а добавленный сценарий должен быть покрыт тестами.

### 5.1. Реализовать регистрацию и хранение паролей

- [ ] Добавить `PasswordEncoder` на основе BCrypt.
- [ ] Создать DTO для регистрации пользователя.
- [ ] Нормализовать email перед поиском и сохранением.
- [ ] Проверять уникальность email.
- [ ] Хешировать пароль перед сохранением пользователя.
- [ ] Реализовать `POST /users/register`.
- [ ] Никогда не сохранять и не логировать исходный пароль.
- [ ] Добавить тест успешной регистрации.
- [ ] Добавить тест регистрации с уже занятым email.
- [ ] Проверить, что в PostgreSQL сохраняется BCrypt-хеш, а не исходный пароль.

На этом этапе JWT ещё не нужен. Результатом должен быть безопасно сохранённый
пользователь, который в дальнейшем сможет войти в систему.

### 5.2. Реализовать login и простой access JWT

- [ ] Создать DTO для входа и ответа с access token.
- [ ] Реализовать `POST /users/login`.
- [ ] Находить пользователя по нормализованному email.
- [ ] Проверять пароль через `PasswordEncoder.matches(...)`.
- [ ] Для неверного email и неверного пароля возвращать одинаковый ответ `401`.
- [ ] Добавить настройки issuer, секрета и времени жизни access token.
- [ ] Хранить JWT-секрет во внешней переменной окружения, а не в Git.
- [ ] Для первого варианта использовать HS256.
- [ ] Создать `JwtEncoder` для выпуска access token.
- [ ] Создать `JwtDecoder` для проверки подписи и срока действия access token.
- [ ] Записывать UUID пользователя в claim `sub`.
- [ ] Установить небольшой срок жизни access token, например 15 минут.
- [ ] После успешного входа возвращать access token в JSON.
- [ ] Добавить тесты успешного и неуспешного входа.

После этого этапа авторизация уже должна работать без refresh token: после
истечения access token пользователь временно входит повторно по email и паролю.

### 5.3. Защитить API с помощью Spring Security

- [ ] Создать `SecurityConfig` и `SecurityFilterChain`.
- [ ] Установить `SessionCreationPolicy.STATELESS`.
- [ ] Настроить проверку Bearer JWT через OAuth2 Resource Server.
- [ ] Не создавать собственный JWT-фильтр без необходимости.
- [ ] Разрешить без access token:
  - `POST /users/register`;
  - `POST /users/login`.
- [ ] Для всех остальных endpoint'ов требовать валидный access token.
- [ ] Настроить JSON-ответы `401 Unauthorized` и `403 Forbidden` в формате
  `ErrorResponse`.
- [x] Получать JWT через `@AuthenticationPrincipal Jwt`.
- [x] Получать `userId` из `jwt.getSubject()`.
- [x] Ограничить текущим `userId` получение одной генерации.
- [x] Ограничить текущим `userId` просмотр истории генераций.
- [x] Ограничить текущим `userId` изменение рейтинга.
- [x] Ограничить текущим `userId` удаление генерации.
- [ ] Ограничить текущим `userId` скачивание файла генерации.
- [ ] Добавить тест запроса с валидным access token.
- [ ] Добавить тесты отсутствующего, повреждённого и истёкшего access token.
- [ ] Добавить тесты невозможности получить или изменить чужую генерацию.

### 5.4. Добавить refresh-сессии

Использовать короткоживущий access JWT и долгоживущий случайный refresh token.
Refresh token передавать через cookie с флагами `HttpOnly`, `Secure` и
`SameSite`. В PostgreSQL хранить только SHA-256-хеш refresh token.

Добавить таблицу `refresh_sessions`:

| Поле         | Назначение                                      |
|--------------|-------------------------------------------------|
| id           | UUID refresh-сессии                             |
| userId       | Владелец сессии                                 |
| tokenHash    | SHA-256-хеш текущего refresh token              |
| expiresAt    | Срок действия                                   |
| createdAt    | Время создания                                  |
| lastUsedAt   | Время последнего успешного обновления           |
| revokedAt    | Время отзыва; `null`, пока сессия действительна |

- [ ] Создать Flyway-миграцию для `refresh_sessions`.
- [ ] Добавить внешний ключ на `users(id)` с `ON DELETE CASCADE`.
- [ ] Добавить уникальный индекс на `token_hash`.
- [ ] Создать сущность refresh-сессии и репозиторий.
- [ ] Генерировать refresh token через `SecureRandom`.
- [ ] Использовать не менее 256 бит энтропии.
- [ ] Перед сохранением вычислять SHA-256-хеш refresh token.
- [ ] Не сохранять и не логировать исходный refresh token.
- [ ] При успешном login создавать refresh-сессию.
- [ ] Устанавливать исходный refresh token в cookie `refresh_token`.
- [ ] Ограничить срок жизни refresh token, например 30 днями.
- [ ] Настроить CORS для frontend и передачу cookie с credentials.
- [ ] Учесть CSRF для endpoint'ов, использующих refresh cookie: настроить
  `SameSite` и проверять разрешённый `Origin`.

### 5.5. Реализовать обновление токенов

- [ ] Разрешить без access token `POST /users/refresh`.
- [ ] Получать refresh token из cookie.
- [ ] Вычислять SHA-256-хеш полученного refresh token.
- [ ] Находить активную refresh-сессию по хешу.
- [ ] Проверять `expiresAt` и `revokedAt`.
- [ ] Выполнять проверку и обновление сессии в одной транзакции.
- [ ] Блокировать строку сессии, чтобы один refresh token нельзя было успешно
  использовать одновременно в двух запросах.
- [ ] При каждом успешном refresh генерировать новый refresh token.
- [ ] Заменять `tokenHash` и обновлять `lastUsedAt`.
- [ ] Выпускать новый access JWT.
- [ ] Возвращать access token в JSON и заменять refresh cookie.
- [ ] Возвращать `401` и очищать cookie для отсутствующего, истёкшего,
  отозванного или уже использованного refresh token.
- [ ] Добавить тест успешного refresh.
- [ ] Проверить, что старый refresh token нельзя использовать повторно.
- [ ] Проверить параллельное использование одного refresh token.

### 5.6. Реализовать logout

- [ ] Разрешить без access token `POST /users/logout`.
- [ ] Получать refresh token из cookie.
- [ ] Находить refresh-сессию по хешу токена.
- [ ] Устанавливать `revokedAt` для найденной сессии.
- [ ] Очищать refresh cookie через `Max-Age=0`.
- [ ] Возвращать `204 No Content`.
- [ ] Сделать logout идемпотентным: повторный запрос также должен завершаться
  успешно.
- [ ] Добавить тест отзыва refresh-сессии.
- [ ] Добавить тест очистки refresh cookie.
- [ ] Добавить тест повторного logout.

В первой версии не добавлять blacklist access JWT. После logout уже выданный
access token может действовать до окончания своего короткого срока жизни. Если
позже потребуется мгновенный отзыв, можно добавить blacklist по claim `jti` или
проверку версии сессии на каждом запросе.

### 5.7. Реализовать смену пароля и удаление аккаунта

- [ ] Реализовать `PUT /users/change-password`.
- [ ] Проверять текущий пароль перед сменой.
- [ ] Сохранять BCrypt-хеш нового пароля.
- [ ] После смены пароля отзывать все refresh-сессии пользователя.
- [ ] Реализовать удаление аккаунта.
- [ ] При удалении аккаунта каскадно удалять refresh-сессии.
- [ ] Добавить тесты смены пароля и отзыва всех refresh-сессий.

### 5.8. Усилить защиту после готовности основного сценария

- [ ] Добавить ограничение количества попыток входа.
- [ ] Добавить периодическое удаление истёкших и отозванных refresh-сессий.
- [ ] Проверить, что пароли и токены отсутствуют в логах.
- [ ] Добавить аудит входов, обновлений токена и выходов без записи токенов.
- [ ] При необходимости добавить просмотр и отзыв активных сессий пользователя.
- [ ] При необходимости добавить обнаружение повторного использования старого
  refresh token и отзыв всей связанной группы сессий.

## 6. Подключить RabbitMQ

- [ ] Определить формат сообщения с заданием на генерацию.
- [ ] Определить формат сообщения об успешном завершении.
- [ ] Определить формат сообщения об ошибке генерации.
- [ ] Настроить exchange, очереди и routing keys.
- [ ] Настроить сериализацию сообщений.
- [ ] Реализовать publisher заданий в backend.
- [ ] После создания запроса отправлять его в RabbitMQ.
- [ ] Добавить версию в контракт сообщений для будущих изменений.
- [ ] Для первой версии проверить простую отправку из сервиса.
- [ ] После готового MVP перейти на transactional outbox.

## 7. Реализовать worker

- [ ] Создать отдельный Python/FastAPI ML-микросервис `worker`.
- [ ] Настроить Python-зависимости и конфигурацию worker.
- [ ] Добавить FastAPI health endpoint.
- [ ] Подключить worker к RabbitMQ.
- [ ] Реализовать listener заданий на генерацию.
- [ ] Согласовать JSON-контракты RabbitMQ между Java backend и Python worker.
- [ ] После получения задания отправлять в backend событие о переходе генерации в статус `PROCESSING`.
- [ ] Добавить общий интерфейс генератора контента.
- [ ] Реализовать хотя бы один тестовый или настоящий генератор изображений.
- [ ] Затем добавить генераторы аудио и видео.
- [ ] После успешной генерации отправлять сообщение о завершении.
- [ ] После ошибки отправлять сообщение со статусом `FAILED`.
- [ ] Собрать worker в отдельный Docker-образ.

## 8. Подключить S3/MinIO

- [ ] Поднять MinIO в локальном окружении.
- [ ] Создать bucket для результатов генерации.
- [ ] Настроить S3-клиент.
- [ ] Определить схему формирования `objectKey`.
- [ ] Реализовать загрузку файла worker'ом.
- [ ] Сохранять в PostgreSQL только ключ и метаданные объекта.
- [ ] Реализовать получение файла backend'ом.
- [ ] Реализовать удаление объекта.
- [ ] Правильно устанавливать `Content-Type`, `Content-Length` и имя файла.
- [ ] Добавить поддержку HTTP Range для аудио и видео после готовности основного сценария.

## 9. Обработать результат генерации

- [ ] Реализовать listener результата в backend.
- [ ] При успехе сохранить `GeneratedAsset`.
- [ ] При успехе изменить статус запроса на `COMPLETED`.
- [ ] Заполнить `completedAt`.
- [ ] Сохранять ассет и менять статус в одной транзакции.
- [ ] При ошибке изменить статус на `FAILED`.
- [ ] Сделать обработку повторных сообщений идемпотентной.
- [ ] Не создавать второй ассет при повторной доставке одного сообщения.

## 10. Добавить надёжность

- [ ] Настроить retry для временных ошибок RabbitMQ и S3/MinIO.
- [ ] Добавить dead-letter queue.
- [ ] Определить максимальное количество повторов.
- [ ] Не подтверждать сообщение RabbitMQ до успешной обработки.
- [ ] Обработать случай, когда файл загружен, но сообщение о завершении не доставлено.
- [ ] При удалении генерации согласованно удалять запись и объект из S3/MinIO.
- [ ] Реализовать transactional outbox для надёжной отправки заданий.

## 11. Добавить наблюдаемость

- [ ] Добавить структурированные логи.
- [ ] Передавать `generationId` во все связанные логи.
- [ ] Подключить Micrometer и Prometheus endpoint в Java backend.
- [ ] Подключить Prometheus-метрики в Python/FastAPI worker.
- [ ] Добавить Prometheus в локальное окружение и настроить сбор метрик backend и worker.
- [ ] Добавить метрики количества задач по статусам.
- [ ] Добавить метрики длительности генерации.
- [ ] Добавить метрики ошибок RabbitMQ и S3/MinIO.
- [ ] Добавить Grafana и подключить Prometheus как источник данных.
- [ ] Создать Grafana dashboard для HTTP-запросов, генераций, RabbitMQ, S3/MinIO и ошибок.
- [ ] Настроить OpenTelemetry в Java backend.
- [ ] Настроить OpenTelemetry в Python/FastAPI worker.
- [ ] Передавать trace context через заголовки сообщений RabbitMQ между backend и worker.
- [ ] Добавить Jaeger для хранения и просмотра распределённых трассировок.
- [ ] Проверить единую трассу `HTTP → backend → RabbitMQ → worker → S3/MinIO → RabbitMQ → backend`.
- [ ] Настроить health checks для PostgreSQL, RabbitMQ и S3/MinIO.
- [ ] Добавить Prometheus, Grafana и Jaeger в `docker-compose`.

## 12. Провести итоговое тестирование

- [ ] Добавить интеграционные тесты PostgreSQL через Testcontainers.
- [ ] Добавить интеграционные тесты RabbitMQ.
- [ ] Добавить интеграционные тесты S3/MinIO.
- [ ] Проверить полный сценарий от создания запроса до скачивания результата.
- [ ] Проверить повторную доставку сообщений.
- [ ] Проверить недоступность RabbitMQ.
- [ ] Проверить недоступность S3/MinIO.
- [ ] Проверить ошибку генератора.
- [ ] Проверить удаление пользователя вместе с его генерациями и файлами.

## Ближайший порядок работы

1. Завершить и протестировать JDBC-репозитории.
2. Реализовать исключения и глобальный обработчик.
3. Реализовать `GenerationService`.
4. Реализовать `GenerationController`.
5. Настроить авторизацию и проверки владельца.
6. Подключить RabbitMQ publisher.
7. Создать Python/FastAPI worker с тестовой генерацией.
8. Подключить MinIO.
9. Реализовать обработку результата в backend.
10. Реализовать скачивание файла.
11. Добавить retry и dead-letter queue.
12. Добавить transactional outbox.
13. Добавить observability.
